<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.ReactNative.AppCompat.Light">
        <item name="android:textColor">#000000</item>
        <item name="android:navigationBarColor">@color/dark</item>
    </style>

    <!-- Application splash screen theme. -->
    <style name="SplashTheme" parent="SplashScreen_SplashTheme">
        <item name="colorPrimaryDark">@color/dark</item>
    </style>

    <!-- Application splash screen theme. -->
    <style name="ARTheme" parent="Theme.Design.NoActionBar">
        <item name="colorPrimaryDark">@color/dark</item>
    </style>

</resources>
