import { requireNativeComponent } from 'react-native';

export const NativeARCameraView = requireNativeComponent('ARCameraFragment');
export const NativeARView = requireNativeComponent('ARFragment');
export const NativeMapView = requireNativeComponent('MapView');
